interface:
  display_name: "Codex Delegate"
  short_description: "Route bounded work through evidence-gated autonomous delegation"
  default_prompt: "Use $codex-delegate with one persistent lead session. Reuse its context for investigation, implementation and repairs; delegate only independent bounded work and finish with fresh Sol review and required evidence."

policy:
  allow_implicit_invocation: true
